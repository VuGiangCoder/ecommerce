const db = require('../models/index');
const RESPONSE = require('../schema/response');
const image = require('../service/image');

const adminController = {
  getUser(req, res) {

  },
  updateUser(req, res) {
    //có thể ban bỏ client để ko thể đặt hàng nữa
  },
  getOrderOwnUser(req, res) {

  },
  changeLimitShopOwnUser(req, res) {

  },
  notifyToUser(req, res) {
    //send notify to user by position or all
  },
  banShop(req, res) {

  },
}